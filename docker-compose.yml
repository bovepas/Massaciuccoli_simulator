version: "3.9"

services:
  massaciuccoli_app:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: massaciuccoli_app
    stdin_open: true
    tty: true
    volumes:
      - ./app:/app
    command: python -m versions.v6_1_orchestrator